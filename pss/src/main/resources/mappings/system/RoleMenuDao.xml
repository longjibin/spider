<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nmw.pss.module.system.dao.RoleMenuDao">
    <resultMap type="RoleMenu" id="entityResult">
    	<id property="id" column="id"/>
    	<result property="createTime" column="create_time"/>
    	<result property="updateTime" column="update_time"/>
    	<result property="createUserId" column="create_user"/>
    	<result property="updateUserId" column="update_user"/>
    	<result property="remark" column="remark"/>
    	<result property="delSign" column="del_sign"/>
    	
    	<result property="roleId" column="role_id"/>
    	<result property="menuId" column="menu_id"/>
    </resultMap>
    
	<insert id="insert">
		INSERT INTO role_menu(id, create_time, update_time, create_user, update_user, remark, del_sign,
			role_id, menu_id) 
		VALUES (#{id}, #{createTime}, #{updateTime}, #{createUserId}, #{updateUserId}, #{remark}, #{delSign}, 
		#{roleId}, #{menuId})
	</insert>
	
	<delete id="deleteById">
		DELETE FROM role_menu WHERE id = #{id}
	</delete>
	
	<select id="selectById" resultMap="entityResult">
		SELECT 
			*
		FROM 
			role_menu 
		WHERE 
			id = #{id}
	</select>
	
	<update id="updateById">
		UPDATE role_menu 
		SET 
			update_time = #{updateTime}, 
			update_user = #{updateUserId}, 
			remark = #{remark}, 
			del_sign = #{delSign}, 
			
			role_id = #{roleId},
			menu_id = #{menuId}
		WHERE id = #{id}
	</update>
	
	<select id="selectByModel" resultMap="entityResult">
		SELECT 
			*
		FROM 
			role_menu 
		<where>
			del_sign=#{DEL_NORMAL}
			<if test="roleId != null and roleId != ''">
				AND role_id=#{roleId}
			</if>
			<if test="menuId != null and menuId != ''">
				AND menu_id=#{menuId}
			</if>
		</where>
	</select>
	
</mapper>