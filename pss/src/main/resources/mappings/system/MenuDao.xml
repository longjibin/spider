<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nmw.pss.module.system.dao.MenuDao">
    
	<insert id="insert">
		INSERT INTO menu(id, create_time, update_time, create_user, update_user, remark, del_sign,
			name, sort, url, icon, parent_id) 
		VALUES (#{id}, #{createTime}, #{updateTime}, #{createUserId}, #{updateUserId}, #{remark}, #{delSign}, 
		#{name} ,#{sort} ,#{url} ,#{icon} ,#{parentId})
	</insert>
	
	<delete id="deleteById">
		DELETE FROM menu WHERE id = #{id}
	</delete>
	
	<select id="selectById" resultType="Menu">
		SELECT 
			*
		FROM 
			menu 
		WHERE 
			id = #{id}
	</select>
	
	<update id="updateById">
		UPDATE menu 
		SET 
			update_time = #{updateTime}, 
			update_user = #{updateUserId}, 
			remark = #{remark}, 
			del_sign = #{delSign}, 
			
			name = #{name},
			sort = #{sort},
			url = #{url},
			icon = #{icon},
			parent_id = #{parentId}
		WHERE id = #{id}
	</update>
	
	<select id="selectByModel" resultType="Menu">
		SELECT 
			*
		FROM 
			menu 
		<where>
			del_sign=#{DEL_NORMAL}
			<if test="parentId != null and parentId != ''">
				AND parent_id=#{parentId}
			</if>
		</where>
	</select>
	
</mapper>