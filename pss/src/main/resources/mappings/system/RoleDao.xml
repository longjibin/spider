<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nmw.pss.module.system.dao.RoleDao">
    
	<insert id="insert">
		INSERT INTO role(id, create_time, update_time, create_user, update_user, remark, del_sign,
			name) 
		VALUES (#{id}, #{createTime}, #{updateTime}, #{createUserId}, #{updateUserId}, #{remark}, #{delSign}, 
		#{name})
	</insert>
	
	<delete id="deleteById">
		DELETE FROM role WHERE id = #{id}
	</delete>
	
	<select id="selectById" resultType="Role">
		SELECT 
			*
		FROM 
			role 
		WHERE 
			id = #{id}
	</select>
	
	<update id="updateById">
		UPDATE role 
		SET 
			update_time = #{updateTime}, 
			update_user = #{updateUserId}, 
			remark = #{remark}, 
			del_sign = #{delSign}, 
			
			name = #{name}
		WHERE id = #{id}
	</update>
	
	<select id="selectByModel" resultType="Role">
		SELECT 
			*
		FROM 
			role 
		<where>
			del_sign=#{DEL_NORMAL}
			<if test="name != null and name != ''">
				AND name=#{name}
			</if>
		</where>
	</select>
	
</mapper>